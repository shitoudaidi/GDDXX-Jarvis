# GDDXX-Jarvis 迭代记录：2026-07-21 / Round 05

时间：19:22-19:29（Asia/Shanghai）

本轮原则：减少重复仪表，把视觉注意力还给 Jarvis 实体与当前动作。

## 已发现并解决的问题（10/10）

1. **中心左右重复显示核心、模型、语音、记忆、队列和链路。** 隐藏中心重复读数，能力详情保留在右侧。
2. **中心底部 6 项状态再次重复右侧能力信息。** 移除重复状态条。
3. **LOCAL、MODE、SCAN 技术脚注增加噪声但没有操作价值。** 从主视觉隐藏该脚注。
4. **实时语音波形被重复状态挤压。** 波形区下移并获得统一的 24px 安全边距。
5. **收起文字输入后，底栏操作仍偏在左侧且隐藏控件参与轨道计算。** 隐藏控件退出布局，五个可见控件以中轴居中。
6. **麦克风按钮尺寸过大，压过实体主体。** 从 64px 收敛到 56px。
7. **待命波形保持较强存在感，容易误以为正在录音。** 待命透明度降至 0.18，仅活跃时恢复。
8. **最小窗口的粒子实体过度填满舞台。** 1180px 以下将实体视觉缩放到 82%。
9. **所有面板边框同权重，聆听、思考、播报只靠小字区分。** 强化面板标题层级，并用青、金、绿状态边区分工作阶段。
10. **顶部模型或设备详情截断后无法查看完整内容。** 状态胶囊增加完整悬停说明。

## 验证

- `npm run build:ui`：通过。
- `npm run probe:layout`：五个场景全部通过。
- 新增视觉层级断言：操作组居中、重复遥测隐藏、详情可访问、最小实体缩放，全部为真。
- 标准和最小窗口截图人工复核：无重叠、裁切和视觉失衡。
